export interface Students {
  id_student: number;
  nama: string;
  age: number;
  class: string;
  exam_score: number;
  enrollment_date: number;
}

// Dummy data or a database connection can be simulated here
let products: Students[] = [];

export default products;
